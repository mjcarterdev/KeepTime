const PageTitle = ({ title }) => {
  return (
    <h1 className="text-3xl font-bold underline py-9 md:text-4xl lg:text-5xl decoration-primary decoration-3">
      {title}
    </h1>
  );
};

export default PageTitle;
